services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: order_flow
      POSTGRES_USER: order_flow
      POSTGRES_PASSWORD: order_flow
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:

